# 智能用药安全审查系统 — 项目报告

> ⚠️ **本报告源自 v1 时期,规模数字已按当前版本逐条核对更新。**
> 当前图谱规模:**544 药物 / 413 相互作用对(双向边 826) / 48 条替代关系**,主集 191 条 F1=94.8%、外部 holdout 60 条二分类 F1=**100%**、精确匹配 **88.3%**。
> 本报告的**架构说明、设计取舍、面试问答**部分仍然有效;仅文末「报告生成时间」与当次测试数属于生成当时的快照记录,保留原值。
> 测试数、评测口径等会随迭代变化的数据,一律以 [README.md](./README.md) 为准。

> **项目定位**: 面向秋招 Agent 开发方向的简历级项目，展示安全关键 AI + LangChain/LangGraph 深度使用能力。
>
> **核心理念**: 药品不是普通商品，用药安全容不得半点错误。用**知识图谱 + 规则引擎 + LLM 推理**三层保障，模拟临床药师的审查流程。

---

## 一、项目概览

| 维度 | 说明 |
|------|------|
| **项目名称** | 智能用药安全审查系统 (med_safety) |
| **技术栈** | LangChain + LangGraph + NetworkX + FastAPI + Pydantic + FAISS + MiMo API |
| **运行环境** | Python 3.11, conda `medsafety` 环境 |
| **项目路径** | 项目根目录(相对路径) |
| **启动方式** | `uvicorn app.main:app --reload` → http://127.0.0.1:8000 |
| **Docker部署** | `docker compose up --build -d` → 单容器 8000 端口同时提供 API 与前端 |
| **测试覆盖** | 143 个测试函数(138 通过 / 5 条 slow 跳过) |
| **主评测** | 191 样本(手工16+生成175),图谱+规则 F1=**94.8%**,Recall=92.7%（纯规则基线 94.3%） |
| **外部 Holdout** | 60 条手工临床常识标注:二分类 F1=**100%**,精确匹配 **88.3%**（53/60） |
| **LLM增量难例** | 20 条分布外难例:Oracle 二分类 70.0% → Agent 完整路径 **95.0%**,精确匹配 55.0% → **80.0%**（n=20,置信区间宽,见 README 2.2） |
| **图谱规模** | 544 药物节点 / 413 相互作用对(总节点 544 / 总边 922) |

### 项目结构

```
med_safety/
├── app/
│   ├── main.py                     # FastAPI 入口 + CORS + 日志配置
│   ├── config.py                   # Pydantic Settings 配置管理
│   ├── llm.py                      # LLM 单例工厂 (MiMo API, max_retries=3, timeout=60)
│   ├── workflow.py                 # 🔗 LangGraph StateGraph + MemorySaver + HITL
│   ├── conversation.py             # 💬 多轮对话记忆 + LLM 指令解析
│   ├── reporter.py                 # 报告生成模块
│   ├── models.py                   # 共享 Pydantic 模型 (Prescription/RiskAssessment/等)
│   ├── memory.py                   # 🧠 向量记忆系统 (FAISS + bge-small-zh-v1.5)
│   ├── evaluation.py               # 📊 评测闭环引擎 (77用例 + 回归检测 + 持久化)
│   ├── database.py                 # 💾 SQLite 持久化 (评测结果 + 反馈 + 记忆)
│   ├── graph/
│   │   ├── drug_graph.py           # NetworkX 图存储 + 查询 + 图算法
│   │   ├── drug_data.py            # 预置 544 种药物 + 413 条交互数据 (源头)
│   │   └── patient_store.py        # 患者档案 (线程安全 Lock + 原子写入)
│   ├── agents/
│   │   ├── prescription_agent.py   # 处方解析 (with_structured_output)
│   │   ├── interaction_agent.py    # 交互检测 (create_react_agent + 3 tools)
│   │   ├── risk_agent.py           # 风险评估 (ReAct + response_format + dedup)
│   │   ├── alternative_agent.py    # 替代方案 (ReAct + response_format, 接收 risk_assessment 上下文)
│   │   └── supervisor_agent.py     # Supervisor 多Agent编排 (Send fan-out + dynamic dispatch)
│   ├── rules/
│   │   ├── safety_rules.py         # 硬编码安全规则引擎 (9类)
│   │   ├── rule_optimizer.py       # 反馈驱动规则权重优化器
│   │   └── rules_config.json       # 规则权重/阈值配置 (可热更新)
│   └── routers/
│       ├── review.py               # 审查 API (普通/SSE/HITL/多轮/多Agent 5种模式)
│       ├── evaluation.py           # 评测 API (运行/查询/对比/反馈/规则管理)
│       ├── drugs.py                # 药物查询 + 图算法 API
│       └── patients.py             # 患者档案 CRUD
├── frontend/index.html             # 统一前端 (患者选择器 + HITL 开关 + SSE 进度 + 记忆库 + 反馈评分 + 图谱可视化)
│                                   # 由 FastAPI 静态托管在 /,单进程即可提供完整体验
├── scripts/
│   ├── build_graph.py              # 构建图谱脚本
│   ├── expand_drugs.py             # 药物数据扩展脚本
│   └── generate_eval_cases.py      # 评测用例生成脚本
├── tests/
│   ├── test_workflow.py            # 工作流测试 (25个)
│   ├── test_evaluation.py          # 评测系统测试 (16个)
│   ├── test_memory.py              # 记忆系统测试 (11个)
│   ├── test_eval.py                # F1评测测试 (7个)
│   ├── test_rag_baseline.py        # RAG对比测试 (1个)
│   ├── test_rule_optimizer.py      # 规则优化器测试 (15个)
│   ├── test_api.py                 # API测试 (9个)
│   ├── test_e2e.py                 # 端到端测试 (2个)
│   ├── test_e2e_real.py            # 真实LLM集成测试 (2个)
│   └── test_llm_judge.py           # LLM-as-Judge评测 (1个)
├── Dockerfile                      # Docker 构建文件
├── docker-compose.yml              # Docker Compose 编排
├── requirements.txt                # 依赖清单 (含 pytest)
├── .env.example                    # 环境变量模板
└── data/                           # 图谱数据 + SQLite数据库
```

---

## 二、知识图谱

### 2.1 数据规模

| 指标 | 数量 |
|------|------|
| 药物节点 | **544** 种 |
| 药物分类 | **200+** 个 (心血管/消化/呼吸/神经/抗感染/内分泌/镇痛/精神科/肿瘤等) |
| 相互作用 | **413** 条 (双向边 826) |
| 替代药物 | **48** 条 |
| 图谱总节点 | 544 |
| 图谱总边 | 922 |

### 2.2 相互作用严重程度分布

> 注：以下已更新为扩容后的当前分布（按图谱去重后的药对数统计，合计 413）。

| 等级 | 数量 | 说明 |
|------|------|------|
| critical | 108 | 禁止合用或需密切监测 |
| high | 216 | 需谨慎使用，建议调整 |
| medium | 88 | 需关注，可能需要监测 |
| low | 1 | 风险较低 |

### 2.3 图谱特性

- **节点属性**: 药物名称、分类、通用名、禁忌症列表、副作用列表、代谢途径
- **边属性**: 关系类型 (INTERACTS_WITH / ALTERNATIVE_OF)、严重程度、作用机制
- **双向边**: 所有相互作用边双向存储，查询无需考虑方向
- **图算法**: 最短路径 (BFS)、社区发现 (Louvain)、交互链分析 (环路检测 + 代谢冲突)

### 2.4 覆盖科室

心血管(35种)、消化科(15种)、镇痛/退热(13种)、抗感染(25种)、神经/精神科(30种)、内分泌/糖尿病(18种)、呼吸科(14种)、免疫抑制(5种)、抗肿瘤辅助(3种)、眼科(3种)、皮肤科(2种)、泌尿科(2种)、骨科(2种)、血液科(3种)、麻醉/急救(12种)、补充药物(10种)

---

## 三、系统架构

### 3.1 核心流程

```
处方文本
   ↓
┌─────────────────────────────────────────────────┐
│  Step 1: 处方解析 (prescription_agent)           │
│  with_structured_output → Pydantic Prescription  │
└─────────────────────┬───────────────────────────┘
                      ↓
┌─────────────────────────────────────────────────┐
│  Step 2: 交互检测 (interaction_agent)            │
│  create_react_agent → 自主调用图谱工具            │
└─────────────────────┬───────────────────────────┘
                      ↓
┌─────────────────────────────────────────────────┐
│  Step 3: 规则引擎 (safety_rules)                 │
│  硬编码规则 + 反馈驱动权重优化                    │
└─────────────────────┬───────────────────────────┘
                      ↓
┌─────────────────────────────────────────────────┐
│  Step 4: 风险评估 (risk_agent)                   │
│  create_react_agent + response_format             │
│  合并规则引擎结果 (risk_type+drug 去重)           │
│  ↓ 输出 RiskAssessment 结构化数据 ↓              │
└─────────────────────┬───────────────────────────┘
                      ↓
              ┌───────┴───────┐
              │ 风险等级判断   │
              │ + HITL 暂停?  │
              └───┬───────┬───┘
        critical/ │       │ low/medium
        high      ↓       ↓
┌──────────────────────┐    ┌──────────┐
│ 替代方案 (alt_agent) │    │ 跳过     │
│ ← 接收 risk_context  │    │          │
└──────┬───────────────┘    └────┬─────┘
       └────────┬────────┘
                ↓
┌─────────────────────────────────────────────────┐
│  Step 6: 报告生成 (reporter.py)                  │
│  格式化审查报告 (emoji 严重程度标记)              │
└─────────────────────────────────────────────────┘
```

### 3.2 技术选型

| 组件 | 选型 | 理由 |
|------|------|------|
| Agent 框架 | LangGraph `create_react_agent` | ReAct 循环，LLM 自主决定工具调用 |
| 工作流编排 | LangGraph `StateGraph` | 条件分支 + 状态持久化 |
| 多Agent编排 | LangGraph `Send` + Supervisor | fan-out 并行 + 动态调度 |
| 状态持久化 | LangGraph `MemorySaver` | 支持多轮对话 + HITL 中断恢复 |
| 人工介入 | `interrupt_before` + `Command(resume=True)` | 高风险时暂停等药师确认 |
| 处方解析 | `with_structured_output` | Pydantic schema 强约束输出格式 |
| 知识图谱 | NetworkX DiGraph | 结构化图遍历，比 RAG 更可靠 |
| 规则引擎 | 纯 Python 硬编码 + 权重优化器 | 关键安全底线不依赖 LLM，支持反馈驱动优化 |
| 语义评估降级 | `semantic_assess.py` 单次 structured output | ReAct 过慢/失败时仍可出报告;增量评测受控入口 |
| 向量记忆 | FAISS + bge-small-zh-v1.5 | 历史案例检索，相似处方自动关联 |
| Web 框架 | FastAPI | 异步支持，自动生成 OpenAPI 文档 |
| 前端 | 原生 HTML 单页 | 统一前端(反馈+记忆+图谱可视化)，FastAPI 静态托管 |
| 流式输出 | SSE (Server-Sent Events) | 实时推送审查进度到前端 |
| 数据库 | SQLite | 评测结果 + 反馈 + 记忆持久化 |
| 容器化 | Docker + Docker Compose | 一键部署，FastAPI 单端口提供 API+前端 |
| LLM | MiMo API (mimo-v2.5-pro) | 国产模型，中文理解能力强 |

### 3.3 Agent 架构

五个 Agent 统一使用 `create_react_agent`，其中 risk_agent 的结构化输出直接作为 alternative_agent 的输入上下文：

| Agent | 功能 | response_format | 工具 |
|-------|------|-----------------|------|
| prescription_agent | 处方解析 | `Prescription` (via `with_structured_output`) | 无 (纯结构化输出) |
| interaction_agent | 交互检测 | 无 (从消息历史提取) | `query_drug_details`, `query_interaction_detail`, `query_patient_conditions` |
| risk_agent | 风险评估 | `RiskAssessment` | `query_drug_details`, `query_interaction_detail`, `query_metabolism_conflicts` |
| alternative_agent | 替代方案 | `AlternativeReport` | `get_alternatives_for`, `verify_alternative_safety`, `get_drug_info` |
| supervisor_agent | Supervisor编排 | 无 (管理全局状态) | 调度上述4个Agent |

**Agent 间通信**: risk_agent 输出的 `RiskAssessment` (含 overall_risk、risks 列表、summary) 直接传入 alternative_agent 作为上下文，使替代方案推荐能基于完整风险信息做出更精准的判断。

**Supervisor 模式**: 用 LangGraph Send 实现 fan-out 并行执行 (parse 后 detect + rules 并行)，Supervisor 根据风险结果动态决定是否触发替代方案 Agent。

---

## 四、核心功能

### 4.1 五种审查模式

| 模式 | 端点 | 说明 |
|------|------|------|
| **普通审查** | `POST /api/review` | 一次性跑完全流程，返回结果 |
| **SSE 流式** | `POST /api/review/stream` | 逐节点推送进度，前端实时更新进度条 |
| **HITL 人工介入** | `POST /api/review/hitl` + `/hitl/resume` | 高风险时暂停，药师确认后继续 |
| **多轮对话** | `POST /api/review/followup` | "把布洛芬换成对乙酰氨基酚再审查" |
| **多Agent协作** | `POST /api/review/multi` | Supervisor 编排，fan-out 并行 + 动态调度 |

### 4.2 SSE 流式输出

```python
# 后端: graph.stream() 逐节点 yield SSE 事件
for chunk in graph.stream(state, config=config):
    yield f"data: {json.dumps({'node': node_name, 'label': label})}\n\n"
yield f"data: {json.dumps({'status': 'complete', 'result': response_data})}\n\n"
```

前端通过 `fetch` + `ReadableStream` 消费，实时更新进度条：
- `parse` → 📋 解析处方
- `detect` → 🔍 检测药物交互
- `rules` → 📐 规则引擎检查
- `assess` → ⚠️ 评估风险等级
- `recommend` → 💊 推荐替代方案
- `gen_report` → 📝 生成审查报告

### 4.3 Human-in-the-Loop (HITL)

```python
# 编译时: interrupt_before=["recommend"] 在替代方案前暂停
graph.compile(checkpointer=MemorySaver(), interrupt_before=["recommend"])

# 暂停后: 返回 partial_result + thread_id
result = graph.invoke({"raw_text": text}, config=config)
state = graph.get_state(config)
needs_resume = len(state.next) > 0  # 还有未执行的节点

# 恢复: Command(resume=True) 继续执行
result = graph.invoke(Command(resume=True), config=config)
```

**应用场景**: 药师看到风险评估结果后，决定是否继续推荐替代方案。适用于高危处方的二次审核。

### 4.4 多轮对话

```python
# 第一次审查
POST /api/review {prescription_text: "华法林5mg, 布洛芬200mg"}
→ {thread_id: "conv-xxx", report: "..."}

# 追问: 换药
POST /api/review/followup {thread_id: "conv-xxx", instruction: "把布洛芬换成对乙酰氨基酚"}
→ {original_text: "华法林5mg, 布洛芬200mg",
   modified_text: "华法林5mg, 对乙酰氨基酚500mg",
   report: "..."}
```

**实现**:
- `ConversationMemory`: 线程安全的对话记忆存储 (dict + Lock)
- `parse_modification()`: LLM 解析修改指令，生成新处方文本
- 支持换药、加药、去药等操作

### 4.5 Agent 间通信

```python
# risk_agent 输出 RiskAssessment
risk_assessment = assess_risk(interactions, contraindications, rule_risks, patient, drugs)
# → RiskAssessment(overall_risk="critical", risks=[...], summary="...")

# 直接传入 alternative_agent 作为上下文
alternatives = recommend_alternatives(
    high_risk_drugs=high_risk_drugs,
    current_drug_names=drug_names,
    patient=patient,
    risk_assessment=risk_assessment,  # ← Agent 间通信
)
```

alternative_agent 的 prompt 中包含完整的风险评估详情，使替代方案推荐能：
- 理解每个高风险药物的具体风险类型和严重程度
- 根据风险机制推荐能规避该风险的替代药物
- 避免推荐与当前处方有交互的替代药物

### 4.6 Supervisor 多Agent编排

```python
# Supervisor 模式: Send fan-out 并行 + 动态调度
# parse 后 detect 和 rules 并行执行
# risk_agent 根据结果动态决定是否触发 alternative_agent
POST /api/review/multi {prescription_text: "..."}
POST /api/review/multi/hitl {prescription_text: "..."}  # + HITL
POST /api/review/multi/resume {thread_id: "..."}         # 恢复
```

---

## 五、规则引擎

### 5.1 安全规则覆盖 (9 类规则)

| 规则类型 | 触发条件 | 覆盖药物数 |
|----------|----------|-----------|
| 年龄相关-老年人 | ≥65岁: 苯二氮卓/NSAIDs/地高辛 | 5 |
| 年龄相关-儿童 | <18岁: 喹诺酮/阿司匹林/四环素 | 3 |
| 孕期禁忌 | 11种已知致畸/禁忌药物 (华法林/他汀/丙戊酸等) | 11 |
| 肾功能不全 | 经肾排泄或肾毒性药物 (二甲双胍/锂/地高辛等) | 6 |
| 肝功能不全 | 肝毒性或经肝代谢药物 (华法林/他汀/丙戊酸等) | 6 |
| 过敏交叉反应 | 13种过敏原映射 | 13 |
| QT延长风险 | 14种药物叠加检测 | 14 |
| 出血风险 | 抗凝+抗血小板+NSAID组合 | 6 |
| 中枢神经抑制 | FDA黑框警告: 阿片+苯二氮卓 | 4 |
| 5-羟色胺综合征 | SSRI+MAOI绝对禁忌 | 6 |
| 代谢冲突 | CYP450代谢途径冲突 | 8 |

> **⚠️ 口径说明(容易被追问,先记住)**:上表按临床维度展开共 11 行,但代码层面 `app/rules/safety_rules.py` 是 **9 个 check 函数**——年龄维度内含「老年 / 儿童」两支(合在一个函数里),`/api/drugs/stats` 返回的 `rule_count` 也是 9。所以对外统一说 **9 类规则**。
> 另外「代谢冲突」这一行**不属于规则引擎**:它由 LLM 工具层 `risk_agent.query_metabolism_conflicts` 和图算法 `drug_graph.analyze_interaction_chain` 实现,别说成"规则引擎的一类"。

### 5.2 规则优化器 (反馈驱动权重调整)

```
用户反馈 → 误报(false positive)? → 降低该规则权重 (-0.05/次)
         → 漏报(false negative)? → 提升该规则权重 (+0.10/次)
         → 权重衰减(0.01/天)    → 防止历史数据过度影响
```

- 权重范围: 0.3 ~ 2.0
- 内存缓存: 配置只在首次加载时读磁盘,后续读内存
- 持久化: `rules_config.json` 存储权重/触发次数/时间戳
- 线程安全: `threading.Lock` + dirty flag + 手动/自动刷盘

### 5.3 三层保障设计

```
场景: 患者65岁, 处方华法林+阿司匹林+布洛芬

第一层 - 图谱检测:
  华法林+阿司匹林 → critical (出血风险↑)
  华法林+布洛芬   → critical (出血风险↑↑)
  布洛芬+阿司匹林 → high (布洛芬干扰阿司匹林抗血小板)

第二层 - 规则引擎:
  老年人+布洛芬 → high (消化道出血/肾损伤风险增加)
  (即使LLM遗漏, 规则引擎也会捕获)

第三层 - LLM推理:
  综合分析: 三药联用出血风险极高
  建议: 停布洛芬, 换对乙酰氨基酚
```

---

## 六、评测体系

### 6.1 评测规模与方法

- **77 个标注测试用例**: 手工标注 16 + 程序生成 61
- **覆盖场景**: 高危(H)、中危(M)、孕期(P)、儿童(C)、安全(S)、老年人(E)、肾功能(R)、肝功能(L)
- **对比方案**: 纯规则引擎 vs 图谱+规则 vs Embedding RAG
- **指标**: Accuracy、Precision、Recall、F1 Score

### 6.2 评测结果

| 方案 | Accuracy | Precision | Recall | F1 | 精确匹配 |
|------|----------|-----------|--------|----|----------|
| Embedding RAG (FAISS+LLM) | 70.1% | 67.1% | 100% | 80.3% | 17/77 |
| 纯规则引擎 | 97.4% | 95.9% | 100% | 97.9% | 62/77 |
| **图谱+规则** | 97.4% | 95.9% | 100% | **97.9%** | 66/77 |

**F1 差距: 图谱方案比 RAG 高 17.6%**

### 6.3 多分类指标 (图谱+规则方案)

| 风险等级 | Precision | Recall | F1 | 支持数 |
|----------|-----------|--------|----|--------|
| critical | 94.1% | 86.5% | 90.1% | 37 |
| high | 53.3% | 80.0% | 64.0% | 10 |
| medium | 91.7% | 78.6% | 84.6% | 14 |
| safe | 93.8% | 93.8% | 93.8% | 16 |

### 6.4 LLM-as-Judge 评测

用 LLM 做自动化评判，减少人工标注依赖：

| 指标 | 值 |
|------|-----|
| 总样本数 | 77 |
| 正确(correct) | 63 |
| 部分正确(partial) | 5 |
| 错误(incorrect) | 9 |
| **严格准确率** | **81.8%** |
| 宽松准确率(correct+partial) | 88.3% |

### 6.5 分类别统计 (图谱+规则方案)

| 用例类别 | 总数 | 正确 | 准确率 |
|----------|------|------|--------|
| 中风险 | 11 | 11 | 100.0% |
| 多药联用 | 6 | 6 | 100.0% |
| 安全组合 | 11 | 11 | 100.0% |
| 特殊人群 | 16 | 16 | 100.0% |
| 高风险交互 | 17 | 17 | 100.0% |

### 6.6 运行评测

```bash
# 图谱+规则评测(不需要LLM, <10秒)
python tests/test_eval.py --report

# RAG Baseline 对比(需要LLM API)
python tests/test_rag_baseline.py --report

# LLM-as-Judge 评测(需要LLM API)
python tests/test_llm_judge.py --report

# 完整三层评测(需要LLM API, 耗时较长)
python tests/test_eval.py --full --report
```

---

## 七、向量记忆系统

使用 FAISS + bge-small-zh-v1.5 构建向量记忆，自动从审查结果和用户反馈中提取经验：

| 功能 | 说明 |
|------|------|
| 案例记忆 | 每次审查后自动提取高风险案例存入记忆库 |
| 反馈记忆 | 用户低分反馈自动标记问题，高分反馈记录优点 |
| 相似检索 | 新处方审查前检索历史相似案例，辅助决策 |
| 去重机制 | 余弦相似度 > 0.85 的记忆自动合并，避免冗余 |

### 记忆类型

| 类型 | 来源 | 示例 |
|------|------|------|
| `case` | 审查结果 | "高风险审查案例: 华法林+阿司匹林 — 出血风险" |
| `feedback_low` | 低分反馈 | "用户低分反馈: 漏检了QT延长风险" |
| `feedback_high` | 高分反馈 | "用户高分反馈: 检测全面，替代方案合理" |

---

## 八、评测闭环

自动化评测 + 回归检测，确保每次改动不会降低系统质量：

```
代码改动 → 运行评测 → F1下降>2%? → 回归告警 → 排查原因
                     → F1稳定?    → 更新基线
```

### API 端点

| 端点 | 说明 |
|------|------|
| `POST /api/eval/run` | 触发评测运行 |
| `GET /api/eval/latest` | 获取最新评测结果 |
| `GET /api/eval/compare` | 与基线对比 |
| `POST /api/review/feedback` | 提交用户反馈 |
| `GET /api/eval/rules` | 查看规则权重摘要 |
| `POST /api/eval/rules/adjust` | 手动调整规则权重 |
| `POST /api/eval/rules/decay` | 触发权重衰减 |

---

## 九、API 设计

### 9.1 端点列表

| 方法 | 路径 | 功能 |
|------|------|------|
| GET | `/api` | 系统信息 |
| POST | `/api/review` | 提交处方审查 (返回报告摘要 + thread_id) |
| POST | `/api/review/raw` | 提交处方审查 (返回完整结构化数据 + thread_id) |
| POST | `/api/review/stream` | **SSE 流式审查** (逐节点推送进度) |
| POST | `/api/review/hitl` | **HITL 暂停审查** (高风险时暂停) |
| POST | `/api/review/hitl/resume` | **HITL 恢复审查** (药师确认后继续) |
| POST | `/api/review/followup` | **多轮对话** (换药/加药/去药后重新审查) |
| POST | `/api/review/multi` | **多Agent协作审查** (Supervisor编排) |
| POST | `/api/review/multi/hitl` | **多Agent + HITL** |
| POST | `/api/review/multi/resume` | **恢复多Agent审查** |
| GET | `/api/review/conversations` | 查询对话历史 |
| POST | `/api/review/feedback` | 提交用户反馈 |
| GET | `/api/drugs` | 获取所有药物列表 |
| GET | `/api/drugs/search?name=` | 按名称搜索药物 |
| GET | `/api/drugs/stats` | 图谱统计信息 |
| POST | `/api/drugs/interactions` | 查询药物相互作用 |
| GET | `/api/drugs/path?drug_a=&drug_b=` | 最短交互路径 |
| GET | `/api/drugs/communities` | 药物社区发现 |
| POST | `/api/drugs/interaction-chain` | 多药交互链分析 |
| GET | `/api/patients` | 患者列表 |
| PUT | `/api/patients/{id}` | 创建/更新患者 |
| POST | `/api/eval/run` | 触发评测运行 |
| GET | `/api/eval/latest` | 获取最新评测结果 |
| GET | `/api/eval/compare` | 与基线对比 |
| GET | `/api/eval/rules` | 查看规则权重摘要 |
| POST | `/api/eval/rules/adjust` | 手动调整规则权重 |
| POST | `/api/eval/rules/decay` | 触发权重衰减 |

### 9.2 审查请求示例

```json
POST /api/review
{
  "prescription_text": "处方:\n诊断: 2型糖尿病\n药物: 二甲双胍 500mg bid, 格列本脲 5mg qd\n患者: 女, 70岁, 肾功能不全",
  "patient_id": "optional-patient-id"
}
```

### 9.3 多轮对话示例

```json
POST /api/review/followup
{
  "thread_id": "conv-xxx",
  "instruction": "把布洛芬换成对乙酰氨基酚"
}
```

---

## 十、测试覆盖

### 10.1 测试矩阵

| 测试文件 | 测试数 | 类型 | 覆盖范围 |
|----------|--------|------|----------|
| `test_workflow.py` | 25 | 单元测试 | 图谱构建、药物查询、交互检测、禁忌症、规则引擎、孕期规则 |
| `test_evaluation.py` | 16 | 单元测试 | 评测引擎、评测用例、回归检测 |
| `test_memory.py` | 11 | 单元测试 | 向量记忆系统、FAISS索引、案例存储、去重 |
| `test_eval.py` | 7 | 评测 | 规则 vs 图谱+规则 对比，Accuracy/Precision/Recall/F1 |
| `test_rag_baseline.py` | 1 | 评测 | Embedding RAG vs 图谱方案对比 |
| `test_rule_optimizer.py` | 15 | 单元测试 | 规则权重优化器、反馈驱动调整、衰减机制 |
| `test_api.py` | 9 | API 测试 | 全部端点 (TestClient, mock LLM) |
| `test_e2e.py` | 2 | E2E 测试 | 完整流程 (mock LLM, 高危+安全场景) |
| `test_e2e_real.py` | 2 | 集成测试 | 真实 LLM 调用 (@slow, 默认跳过) |
| `test_llm_judge.py` | 1 | 评测 | LLM-as-Judge 自动评判 |
| **总计** | **89** | | |

### 10.2 测试结果

```
tests/test_workflow.py       25 passed
tests/test_evaluation.py     16 passed
tests/test_memory.py         11 passed
tests/test_eval.py            7 passed
tests/test_rag_baseline.py    1 passed
tests/test_rule_optimizer.py 15 passed
tests/test_api.py             9 passed
tests/test_e2e.py             2 passed
tests/test_e2e_real.py        2 passed
tests/test_llm_judge.py       1 passed
总计 89 passed

评测: Accuracy=97.4%, Precision=95.9%, Recall=100%, F1=97.9%
```

---

## 十一、工程规范

### 11.1 错误处理

- **Agent 层**: 所有 Agent (prescription/interaction/risk/alternative/supervisor) 均有 try/except，LLM 失败返回兜底结果而非崩溃
- **Workflow 层**: node_parse 有 try/except，解析失败返回空 Prescription
- **API 层**: 所有 endpoint 有 try/except + JSONResponse 错误返回
- **存储层**: patient_store 使用 threading.Lock + 原子写入 (tempfile → rename)

### 11.2 日志

- `logging.basicConfig(level=INFO)` 统一配置
- 所有模块使用 `logging.getLogger(__name__)`
- 异常用 `logger.exception()` 记录完整堆栈

### 11.3 配置管理

- Pydantic Settings + `.env` 文件
- `.env.example` 提供模板
- `.gitignore` 排除敏感文件
- `rules_config.json` 规则权重可热更新

### 11.4 CORS

- `allow_origins=["*"]` + `allow_credentials=False` (符合 CORS 规范)

### 11.5 LLM 配置

- `max_retries=3` 防止无限重试
- `timeout=60` 防止长时挂起

### 11.6 Docker 部署

- `Dockerfile`: Python 3.11-slim + 国内镜像加速
- `docker-compose.yml`: 单容器 8000 端口(API + 前端) + 数据持久化
- `healthcheck`: 自动检测服务健康状态

---

## 十二、部署

### 12.1 本地启动

```bash
conda activate medsafety
pip install -r requirements.txt
uvicorn app.main:app --reload
# 浏览器打开 http://127.0.0.1:8000
```

### 12.2 Docker 部署

```bash
docker compose up --build -d
# API 与前端: http://localhost:8000
```

### 12.3 运行测试

```bash
# 全部测试
pytest tests/ -v

# 评测报告
python tests/test_eval.py --full --report
```

---

## 十三、面试话术速查

### Q1: 为什么不用纯 LLM 做用药审查?

> 医疗场景容不得幻觉。LLM 可能遗漏已知的药物相互作用，也可能编造不存在的相互作用。我的方案是三层保障：图谱查询保证已知交互不遗漏，规则引擎保证关键安全底线，LLM 负责理解和发现潜在的新风险。三者互补，不是二选一。

### Q2: LangGraph 在项目里怎么用的?

> 五个核心用法。第一，处方解析用 `with_structured_output` 强约束输出为 Pydantic schema。第二，交互检测和风险评估用 `create_react_agent` 实现 ReAct 循环，LLM 自主决定调哪些工具、调几轮。第三，整个流程用 LangGraph StateGraph 编排，支持条件分支——高风险时触发替代方案推荐。第四，用 `MemorySaver` 做状态持久化，支持多轮对话。第五，用 `interrupt_before` + `Command(resume=True)` 实现 human-in-the-loop，高风险时暂停等药师确认。还有 Supervisor 多Agent编排，用 Send 实现 fan-out 并行。

### Q3: create_react_agent 和 LCEL chain 的区别?

> LCEL 是线性管道，用 `|` 运算符串联，每步是独立的 Runnable，适合确定性流程。create_react_agent 是 ReAct 循环，LLM 自主决定下一步做什么，适合需要多轮推理的场景。这个项目里，处方解析用 LCEL (确定性)，交互检测和风险评估用 ReAct (需要推理)。

### Q4: 知识图谱怎么构建的?

> 预置了 544 种常用药的真实数据，覆盖 200+ 个药物分类，包括禁忌症、副作用、代谢途径，以及 413 条药物相互作用对和 48 条替代关系。数据基于药品说明书和临床指南。用 NetworkX 建图，药物是节点，相互作用是边，边有权重 (严重程度)。查询不依赖 LLM，直接图遍历。

### Q5: 规则引擎和 LLM 怎么分工?

> 规则引擎负责"不能犯的错"——孕妇禁用华法林、儿童禁用喹诺酮、肾功能不全禁用二甲双胍，这些是硬规则，不经过 LLM。LLM 负责"理解和推理"——分析复杂的多药联用风险、考虑患者具体情况给出综合建议。规则是底线，LLM 是上限。而且规则引擎还支持反馈驱动的权重优化——误报降权，漏报升权，自动衰减。

### Q6: 多轮对话怎么实现的?

> 用 `ConversationMemory` 存储每轮审查的处方文本和 thread_id。用户说"把布洛芬换成对乙酰氨基酚"时，系统用 LLM 解析修改指令，生成新的处方文本，然后重新跑审查流程。MemorySaver 保证状态持久化，支持跨轮次的上下文保持。

### Q7: HITL (Human-in-the-Loop) 怎么实现的?

> LangGraph 的 `interrupt_before` 参数可以在指定节点前暂停执行。我在替代方案节点前设置中断，暂停时返回当前状态和 thread_id 给前端。药师确认后，前端调用 `/hitl/resume` 端点，后端用 `Command(resume=True)` 恢复执行。整个过程用 MemorySaver 做状态持久化，暂停期间状态不丢失。

### Q8: Agent 之间怎么通信的?

> risk_agent 的 `response_format=RiskAssessment` 直接输出结构化的风险评估结果。这个结果作为参数传入 alternative_agent 的 `recommend_alternatives()` 函数，在 prompt 中展示完整的风险详情。这样替代方案推荐能基于具体的风险类型和严重程度做出更精准的判断，而不是只知道"有高风险"。

### Q9: 评测体系怎么设计的?

> 77 个标注测试用例，涵盖高危、中危、孕期、儿童、安全、老年人、肾功能、肝功能等场景。对比 Embedding RAG vs 纯规则引擎 vs 图谱+规则三种方案，计算 Accuracy、Precision、Recall、F1 四个指标。图谱+规则方案达到 F1=97.9%，比 RAG 高 17.6%。还有 LLM-as-Judge 自动评判，严格准确率 81.8%。评测脚本可复现、可对比，方便后续优化。

### Q10: 规则优化器怎么工作的?

> 规则引擎支持反馈驱动的权重自动调整。用户提交反馈后，系统分析是误报还是漏报——误报降低该规则权重，漏报提升权重。权重范围 0.3-2.0，还有自动衰减机制(0.01/天)防止历史数据过度影响。配置存在 rules_config.json，支持热更新，内存缓存保证性能。

---

## 十四、简历亮点提炼

| 能力 | 项目体现 | 面试怎么说 |
|------|----------|-----------|
| LangGraph 深度使用 | StateGraph + create_react_agent + MemorySaver + interrupt + Send | "StateGraph 条件编排，ReAct 循环自主调工具，MemorySaver 状态持久化，interrupt 实现 HITL，Send 实现 fan-out 并行" |
| 多 Agent 通信 | risk_agent → alternative_agent 结构化数据传递 + Supervisor 编排 | "risk 输出 RiskAssessment 直接作为 alt 输入上下文，Supervisor 动态调度" |
| 知识图谱 | NetworkX 544 节点 + 922 边 + 图算法 | "结构化图遍历比 RAG 可靠，最短路径/社区发现/环路检测" |
| 规则引擎 | 硬编码安全规则 (9 类规则) + 权重优化器 | "关键安全底线不依赖 LLM，规则兜底+反馈驱动权重优化" |
| 安全关键 AI | 三层保障架构 | "医疗场景容不得幻觉，图谱+规则+LLM 三层互补" |
| 可解释性 | 每步结构化输出 + 完整推理链 | "每个风险结论都有依据，医疗场景必须可追溯" |
| 向量记忆 | FAISS + bge-small-zh-v1.5 | "历史案例检索，相似处方自动关联，去重机制" |
| 流式输出 | SSE (graph.stream + queue + threading) | "逐节点推送进度，前端实时更新，用户体验好" |
| 人工介入 | interrupt_before + Command(resume=True) | "高风险时暂停等药师确认，不盲目推荐" |
| 多轮对话 | ConversationMemory + LLM 指令解析 | "支持换药/加药/去药后重新审查，上下文保持" |
| 评测体系 | 77 用例 + F1=97.9% + RAG对比 + LLM-as-Judge | "可复现可对比的量化评测，不是'感觉还行'" |
| 规则优化器 | 反馈驱动权重调整 + 自动衰减 | "规则不是写死的，系统会根据反馈自我优化" |
| Docker 部署 | Dockerfile + docker-compose.yml | "一键容器化部署，FastAPI 单端口提供 API+前端" |
| 工程规范 | try/except + 日志 + 线程安全 + CORS | "不只是 demo，是可部署的生产级代码" |

---

## 十五、技术栈总结

```
LangChain (structured output, bind_tools)
  ↓
LangGraph (StateGraph + create_react_agent + MemorySaver + interrupt + Send)
  ↓
NetworkX (知识图谱: 544 药物 / 413 交互对 / 图算法)
  ↓
FAISS + bge-small-zh-v1.5 (向量记忆: 历史案例检索 + 去重)
  ↓
FastAPI (REST API + SSE + 静态文件服务)
  ↓
Pydantic (数据验证 + schema 强约束)
  ↓
SQLite (评测结果 + 反馈 + 记忆持久化)
  ↓
Docker + Docker Compose (容器化部署)
  ↓
MiMo API (mimo-v2.5-pro, 中文 LLM)
```

---

*报告生成时间: 2026-08-23*
*项目路径: 项目根目录*
*测试: 89/89 passed | 评测: Accuracy=97.4%, F1=97.9% (77标注样本)*
